{
  "name": "hcl-agentic-ai-root",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install": "cd frontend && npm install"
  },
  "dependencies": {
    "next": "16.1.6"
  }
}
