version: '3.8'

services:
  backend:
    build: ./backend
    container_name: hcl-agentic-ai-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend:/app
    restart: always
